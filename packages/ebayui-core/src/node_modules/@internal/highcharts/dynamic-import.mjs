export function load() {
    const modleLoader = import("./module-loader");

    return modleLoader.then((modules) => {
        window.Highcharts = modules.highcharts;
        return modules;
    });
}
