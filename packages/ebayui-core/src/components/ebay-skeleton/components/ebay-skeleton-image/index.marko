import { processHtmlAttributes } from "../../../../common/html-attributes";
import type { WithNormalizedProps } from "../../../../global";
static {
    interface SkeletonImageInput extends Omit<Marko.HTML.Div, `on${string}`> {
        as?: keyof Marko.NativeTags;
        class?: Marko.HTMLAttributes["class"];
    }
}
export interface Input extends WithNormalizedProps<SkeletonImageInput> {}
$ const {
    as: inputAs,
    class: inputClass,
    ...htmlInput
} = input;

<${inputAs || "div"}
    class=["skeleton__image", inputClass]
    ...processHtmlAttributes(htmlInput)
/>
